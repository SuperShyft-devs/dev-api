[pytest]
asyncio_mode = auto
# Session-scoped async engine must share one event loop with per-test sessions and
# autouse cleanup; otherwise teardown runs after the per-test loop is closed.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
